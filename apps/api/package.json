{
  "name": "@akela/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "wrangler types --env-interface CloudflareBindings && tsc --pretty false"
  },
  "dependencies": {
    "@akela/core": "*",
    "@akela/identity": "*",
    "@akela/indexers": "*",
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.34.0"
  }
}
