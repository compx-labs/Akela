{
  "name": "akela",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @akela/web",
    "dev:api": "npm run dev -w @akela/api",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint -w @akela/web"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "packageManager": "npm@10.8.2",
  "overrides": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
